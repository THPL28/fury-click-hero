version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: fury-redis
    command: redis-server --save 60 1 --loglevel warning
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
    driver: local
